#ifndef DEF_H
#define DEF_H

#define RT_SUCCEED  (0) // return value for succeed process
#define RT_FAILED  (-1) // return value for failed process

#define TRUE    (1);
#define FALSE   (0);

typedef unsigned int uint;

typedef int VAL;            // only support int for now

#endif